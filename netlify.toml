[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"

# SPA fallback (backup, _redirects takes priority)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
